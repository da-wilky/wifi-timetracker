# Task
- The current project cannot be built, it throws compilation errors. Fix those compilation errors while maintaining the wanted functionality of the application. The App needs to build correctly.
- Also modify the flake.nix file, so that I can run nix build for the application to build and nix run for the application to start on my phone connected with adb.
